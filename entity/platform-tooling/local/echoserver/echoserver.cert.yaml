apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: echoserver
spec:
  dnsNames:
  - echoserver.local.klessen.cloud
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: echoserver-tls
